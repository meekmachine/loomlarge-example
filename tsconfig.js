// NOTE: This file was previously (incorrectly) used as a Vite config.
// The real Vite config is in `vite.config.ts`.
// TypeScript expects a `tsconfig.json` at the project root for type-checking.
// Create one when ready; for now this placeholder prevents accidental use.
export {};